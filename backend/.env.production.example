# Production Environment Variables
# These should be set in Azure App Service Configuration

# Database
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/infera_ai?retryWrites=true&w=majority

# JWT Secret (generate a strong secret)
JWT_SECRET=your-super-secure-jwt-secret-key-here-min-32-characters

# Email Service (if using)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Frontend URL
FRONTEND_URL=https://infera-ai-five.vercel.app

# Node Environment
NODE_ENV=production

# Port (Azure uses 80 or 8080)
PORT=80